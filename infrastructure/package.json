{
  "name": "infrastructure",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest --run",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all --profile prime-deal-auto",
    "diff": "cdk diff --profile prime-deal-auto",
    "destroy": "cdk destroy --all --profile prime-deal-auto"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "cdk-nag": "^2.34.0",
    "constructs": "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.2",
    "fast-check": "^3.23.2",
    "ts-node": "^10.9.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.9"
  }
}
